name: Docker Build Python and upload to Jfrog

on: [push]
  
jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Login to JFrog 🐸  
        uses: docker/login-action@v1
        with:
          registry: nipusa2027.jfrog.io
          username: test
          password: Test20240515

      - name: Build and push 🐳
        id: docker_build
        uses: actions/hello-world-docker-action@v2
        with:
          push: true
          tags: nipusa2027.jfrog.io/python_test:latest
          
